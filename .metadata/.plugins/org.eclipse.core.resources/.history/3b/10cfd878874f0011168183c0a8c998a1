#ifndef APP_I2C_DFU_H
#define APP_I2C_DFU_H

#include <stdint.h>
#include <stdbool.h>

/* I2C 설정 */
#define I2C_SLAVE_ADDR      0xE2        // 7-bit 주소
#define I2C_RX_BUF_SIZE     256         // CMD(1) + LEN(2) + DATA(128) + CRC(1)

/* 슬롯 크기 */
#define SLOT_SIZE           0x80000     // 512KB

/* 커맨드 정의 */
#define CMD_ERASE           0x01
#define CMD_DATA            0x02
#define CMD_VERIFY          0x03
#define CMD_REBOOT          0x04

///* DFU 상태 */
//typedef enum {
//    DFU_IDLE = 0,
//    DFU_RECEIVING,
//    DFU_VERIFIED,
//    DFU_ERROR
//} DFU_State_t;

typedef enum {

};

/* 전역 변수 (app_process.c에서 정의) */
extern volatile uint8_t    i2c_rx_buf[I2C_RX_BUF_SIZE];
extern volatile uint16_t   i2c_rx_len;
extern volatile bool       i2c_pkt_ready;
extern volatile DFU_State_t dfu_state;

#endif /* APP_I2C_DFU_H */
